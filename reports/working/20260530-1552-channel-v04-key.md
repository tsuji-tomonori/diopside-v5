# Channel v0.4 key 対応 作業レポート

## 受けた指示

- `.workspace/plan-20260530.txt` と `.workspace/` の設計書に沿って v0.4 設計準拠を進める。
- main を pull してから worktree / task / PR flow で作業する。

## 要件整理

- `Channel` の新規保存 key を `CH#{channel_id}` / `META` にする。
- v0.4 fields として `channel_title`、`uploads_playlist_id`、`collect_enabled`、`default_tags` を保持する。
- 既存 `CHANNEL#{channel_id}` / `META` は読み取り fallback として維持する。
- `ChannelRef` の同期と管理 channel API の既存 contract を維持する。
- README と schema audit を実装済み範囲に同期する。

## 検討・判断

- `channel_item` helper を追加し、`put_channel` の保存 shape を v0.4 key に寄せた。
- 既存 API / UI 互換のため `enabled`、`display_name` は alias として維持した。
- `get_channel` は新 key を優先し、旧 key を fallback する。
- DynamoDB adapter の旧 Channel scan fallback と既存 data backfill は後続範囲として残した。

## 実施作業

- `channel_item`、`get_channel` を追加し、`put_channel` を `CH#{channel_id}` / `META` 保存に変更した。
- repository schema contract と admin channel API test を v0.4 key に更新した。
- `README.md` と `docs/design/dynamodb-schema-audit.md` の `Channel` 記述を更新した。

## 成果物

- `apps/shared/src/diopside_core/repository.py`
- `tests/test_repository_schema_contract.py`
- `tests/test_api_handler.py`
- `README.md`
- `docs/design/dynamodb-schema-audit.md`
- `tasks/do/20260530-1552-channel-v04-key.md`

## 検証

- `git diff --check` pass
- `python3 -m py_compile apps/shared/src/diopside_core/repository.py apps/api/src/diopside_api/handler.py` pass
- `PYTHONPATH=apps/shared/src python3 -m pytest tests/test_repository_schema_contract.py` pass: 27 passed
- `PYTHONPATH=apps/shared/src:apps/api/src python3 -m pytest tests/test_api_handler.py tests/test_repository_schema_contract.py` pass: 53 passed
- `PYTHONPATH=apps/shared/src:apps/workers/static-exporter/src python3 -m pytest tests/test_core_pipeline.py` pass: 43 passed
- `node tools/check-docs-consistency.mjs` pass
- `npm run verify` pass: 128 tests passed, build/package/e2e local passed

## Fit 評価

- 新規保存は v0.4 key に寄せ、旧 key は fallback で維持したため受け入れ条件に合致。
- `ChannelRef` 同期と管理 channel API の返却 contract は維持している。
- docs は実装済み範囲と未対応範囲を分けて記載した。

## 未対応・制約・リスク

- 既存 DynamoDB data の backfill は未実施。
- DynamoDB adapter の旧 `Channel` item scan fallback は未強化。
- ChannelRef rebuild job は未実装。
